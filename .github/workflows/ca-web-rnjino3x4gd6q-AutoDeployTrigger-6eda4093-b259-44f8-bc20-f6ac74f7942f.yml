name: Trigger auto deployment for ca-web-rnjino3x4gd6q

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/ca-web-rnjino3x4gd6q-AutoDeployTrigger-6eda4093-b259-44f8-bc20-f6ac74f7942f.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.CAWEBRNJINO3X4GD6Q_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CAWEBRNJINO3X4GD6Q_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CAWEBRNJINO3X4GD6Q_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: crrnjino3x4gd6q.azurecr.io
          registryUsername: ${{ secrets.CAWEBRNJINO3X4GD6Q_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CAWEBRNJINO3X4GD6Q_REGISTRY_PASSWORD }}
          containerAppName: ca-web-rnjino3x4gd6q
          resourceGroup: rg-agents-playground-111
          imageToBuild: crrnjino3x4gd6q.azurecr.io/ca-web-rnjino3x4gd6q:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


